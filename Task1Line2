public class Task1Line2 {
    public static void main(String[] args) {
        double a = 2;
        double b = 25;
        double c = 1;
        double z = (b+Math.sqrt(b*b+4*a*c))/(2*a)-Math.pow(a,3)*c+Math.pow(b,-2);
        System.out.println("при а="+a+", b="+b+" c="+c+",\nz="+z);
    }
}
